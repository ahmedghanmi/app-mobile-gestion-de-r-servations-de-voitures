<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:car_management_app.ViewModels"
             x:Class="car_management_app.Views.ClientsPage">
    <ContentPage.BindingContext>
        <vm:ClientViewModel />
    </ContentPage.BindingContext>

    <StackLayout Padding="10">
        <Label Text="Liste des Clients" FontSize="Large" HorizontalOptions="Center" />

        <ListView ItemsSource="{Binding Clients}" ItemTapped="OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Nom}" Detail="{Binding Email}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="Ajouter un client" Command="{Binding AjouterClientCommand}" />
    </StackLayout>
</ContentPage>
